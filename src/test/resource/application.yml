# Test
spring:
  profiles:
    active: local # 기본 환경 선택

# local 환경
---
spring:
  profiles: local
  jpa:
    show-sql: true

---
spring.profiles: set1
server:
  port: 8081

---
spring.profiles: set2
server:
  port: 8082